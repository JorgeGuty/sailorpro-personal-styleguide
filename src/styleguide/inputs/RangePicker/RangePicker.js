

const RangePicker = ({dsl}) => {

    function renderRangePicker() {

        return (
            <div>
                this is a RangePicker
            </div>
        )
    }

    return renderRangePicker()
}
  
export default RangePicker;